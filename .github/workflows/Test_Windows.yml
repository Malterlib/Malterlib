name: Test Windows

on:
  workflow_call:
    inputs:
      platform:
        required: true
        type: string
      architecture:
        required: true
        type: string
      configuration:
        required: true
        type: string
      compiler:
        required: true
        type: string

env:
  Malterlib_UseCachedEnvironment: "false"
  Malterlib_UseUserSettings: "false"
  MalterlibRepositoryHardReset: "true"
  MalterlibPreBuildNoClean: "true"
  MalterlibPreBuildNoReBuild: "true"
  MalterlibDisableBuildSystemGeneration: "true"
  MalterlibEnableLinkTimeCodeGeneration: "false"
  MalterlibImportUpdateCache: "false"
  MalterlibBuildLogFile: "Artifacts/Build.log"
  MalterlibEnableSanitizerReleaseConfigs: "true"
  MalterlibEnablePrefTestConfig: "false"
  MalterlibEnableInternalTests: "true"
  MalterlibDebugSymbolsEnable: "false"
  MalterlibDeploySymbols: "false"
  MalterlibSupportCrashDumps: "false"
  MalterlibFlakySuites: "Malterlib/File/File/Tests;Malterlib/Process/ProcessLaunch/*/*/KillSandbox"
  MalterlibFileLineOnDebugBreak: "true"
  MalterlibReproducibleBuild: "true"
  MalterlibUseClangForWindowsConfigs: "true"
  MalterlibTestsMem_EnablePerfTests: "false"
  RunningCI: "true"
  MALTERLIB_GITHUB_USER_TOKEN: ${{ secrets.MALTERLIB_GITHUB_USER_TOKEN }}
  MALTERLIB_GITHUB_USER: ${{ secrets.MALTERLIB_GITHUB_USER }}
  ASAN_OPTIONS: "detect_leaks=0"
  TSAN_OPTIONS: "second_deadlock_stack=1"

jobs:
  build_windows:
    name: Build ${{ inputs.platform }} ${{ inputs.architecture }} ${{ inputs.configuration }} ${{ inputs.compiler }}
    #runs-on: runner-windows-16core
    runs-on: self-hosted-windows-11-build
    timeout-minutes: 30
    env:
      BuildPlatform: ${{ inputs.platform }}
      BuildArchitecture: ${{ inputs.architecture }}
      BuildConfiguration: ${{ inputs.configuration }}
    steps:
    - name: Setup git config
      run: |
        git config --global core.autocrlf false
        git config --global core.eol lf
        git config --global core.symlinks true
    - uses: actions/checkout@v4
      with:
        lfs: 'true'
        clean: false
        persist-credentials: false
    # - name: Configure Pagefile
    #   shell: pwsh
    #   run: |
    #     [System.UInt64] $MinimumSize = ($env:MinimumSize / 1kb) * 1kb
    #     if ($MinimumSize -le 0) {
    #       $MinimumSize = 8gb
    #     }
    #     [System.UInt64] $MaximumSize = ($env:MaximumSize / 1kb) * 1kb
    #     if ($MaximumSize -le 0) {
    #       $MaximumSize = $MinimumSize
    #     }
    #     [System.String] $DiskRoot = $env:DiskRoot
    #     echo "Pagefile configuration:"
    #     echo "- Minimum size: $($MinimumSize)"
    #     echo "- Maximum size: $($MaximumSize)"
    #     echo "- Disk root: $($DiskRoot)"
    #     Add-Type -Path "$env:SourcePath"
    #     [Util.PageFile]::SetPageFileSize($MinimumSize, $MaximumSize, $DiskRoot)
    #   env:
    #     MinimumSize: 16GB
    #     MaximumSize: 64GB
    #     DiskRoot: "C:"
    #     SourcePath: ${{ github.workspace }}/.github/workflows/SetPageFileSize.cs
    #- name: Cache Binaries
    #  uses: actions/cache@v4
    #  env:
    #    cache-name: cache-binaries-windows-x64
    #  with:
    #    path: |
    #      Binaries/Malterlib/Windows/x64
    #    key: ${{ env.cache-name }}-${{ hashFiles('**/*.MRepo') }}
    #    restore-keys: |
    #      ${{ env.cache-name }}-
    - name: Build
      shell: bash -leo pipefail {0}
      run: |
        set -e
        git reset --hard
        ./.github/workflows/Build.sh
    - uses: actions/upload-artifact@v4
      if: (failure() || success())
      with:
        name: Tests ${{ inputs.platform }} ${{ inputs.architecture }} ${{ inputs.configuration }} ${{ inputs.compiler }}
        path: |
          Artifacts
    - uses: actions/upload-artifact@v4
      if: (success() && (env.MalterlibDeploySymbols == 'true'))
      with:
        name: Symbols ${{ inputs.platform }} ${{ inputs.architecture }} ${{ inputs.configuration }} ${{ inputs.compiler }}
        path: |
          Symbols

  test_windows:
    name: Test ${{ matrix.os }} ${{ inputs.architecture }} ${{ inputs.configuration }} ${{ inputs.compiler }}
    runs-on: ${{ matrix.os }}
    needs: [build_windows]
    timeout-minutes: 70
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2019, windows-2022]
    steps:
    - uses: actions/download-artifact@v4
      with:
        name: Tests ${{ inputs.platform }} ${{ inputs.architecture }} ${{ inputs.configuration }} ${{ inputs.compiler }}
        path: Artifacts
    - name: Disk space before
      shell: bash
      run: |
        echo Workspace: '${{ github.workspace }}'
        df -h
        curl -H 'Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' -H 'Accept: application/vnd.github.v3.raw' -L -O "https://api.github.com/repos/$GITHUB_REPOSITORY/contents/.github/workflows/SetPageFileSize.cs?ref=$GITHUB_REF"
    - name: Configure Pagefile
      shell: pwsh
      run: |
        [System.UInt64] $MinimumSize = ($env:MinimumSize / 1kb) * 1kb
        if ($MinimumSize -le 0) {
          $MinimumSize = 8gb
        }
        [System.UInt64] $MaximumSize = ($env:MaximumSize / 1kb) * 1kb
        if ($MaximumSize -le 0) {
          $MaximumSize = $MinimumSize
        }
        [System.String] $DiskRoot = $env:DiskRoot
        echo "Pagefile configuration:"
        echo "- Minimum size: $($MinimumSize)"
        echo "- Maximum size: $($MaximumSize)"
        echo "- Disk root: $($DiskRoot)"
        Add-Type -Path "$env:SourcePath"
        [Util.PageFile]::SetPageFileSize($MinimumSize, $MaximumSize, $DiskRoot)
      env:
        MinimumSize: 16GB
        MaximumSize: 64GB
        DiskRoot: "C:"
        SourcePath: ${{ github.workspace }}/SetPageFileSize.cs
    - name: Disk space after
      shell: bash
      run: |
        df -h
    - name: Run tests
      shell: bash
      run: |
        set -ex
        if [[ "$MatrixOs" == "windows-2019" ]]; then
          MalterlibFlakySuites="$MalterlibFlakySuites;Malterlib/Cloud/SecretsManager/General"
        fi
        pwd
        echo Num CPU threads: `getconf _NPROCESSORS_ONLN`
        rm -rf Deploy
        mkdir -p Deploy
        cd Deploy
        export MalterlibCrashDumpDir=`cygpath -m "$PWD/CrashDumps"`
        mkdir -p "$PWD/CrashDumps"
        tar -xf ../Artifacts/Deploy.tar.zst
        cd Tests*
        ./RunAllTests --quiet --launch-per-suite --suite-order slow_first --timeout 3600 -- --logs
        exit 0
      env:
        MatrixOs: ${{ matrix.os }}

    - uses: actions/upload-artifact@v4
      if: ((failure() || success()) && (env.MalterlibSupportCrashDumps == 'true'))
      with:
        name: Crash Dumps ${{ inputs.platform }} ${{ matrix.os }} ${{ inputs.architecture }} ${{ inputs.configuration }} ${{ inputs.compiler }}
        path: |
          Deploy/CrashDumps/MiniDump_*
          Deploy/CrashDumps/*.txt
